// #include "../include/Interfaz.h"
#include "../include/Arbol_R_Hilbert.h"
int main()
{
    Interfaz aplicacion;
    aplicacion.ejecutar();
    // Arbol_R_Hilbert a;
    // a.insertar({{169, 472}});
    // a.insertar({{339, 466}});
    // a.insertar({{603, 450}});
    // a.insertar({{763, 378}});
    // a.insertar({{887, 348}});
    // a.insertar({{192, 322}});
    // a.insertar({{725, 521}});
    return 0;
}
